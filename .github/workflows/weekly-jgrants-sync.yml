name: 週次J-Grants同期

on:
  schedule:
    # 毎週月曜日 午前3時（JST）に実行（UTC 18:00 日曜日）
    - cron: '0 18 * * 0'
  workflow_dispatch:  # 手動実行も可能

env:
  NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
  NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

jobs:
  sync-jgrants:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 依存関係インストール
        run: npm ci

      - name: J-Grants API同期
        run: npm run sync:subsidies

      - name: 実行結果サマリー
        if: always()
        run: |
          echo "### 📊 J-Grants週次同期完了" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 実行日時: $(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M:%S JST')" >> $GITHUB_STEP_SUMMARY
          echo "- データソース: J-Grants公式API" >> $GITHUB_STEP_SUMMARY
          echo "- エンドポイント: https://api.jgrants-portal.go.jp/exp/v1/public" >> $GITHUB_STEP_SUMMARY
